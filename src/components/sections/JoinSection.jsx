import { FeatureGrid } from './FeatureGridSection';

export const Join = ({ section }) => <FeatureGrid section={{ ...section, eyebrow: 'Start here', title: 'There is a first useful job for everyone' }} />;
